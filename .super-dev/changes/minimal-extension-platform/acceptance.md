# 最小扩展平台验收记录

## 候选与范围

- 验收日期：2026-08-25
- 分支：`feature/minimal-extension-platform`
- 基础提交：`b7918b506159854b3370346ff99af88c8da1777c`
- 默认状态：扩展关闭，允许的内置方法为空。
- 本阶段只交付扩展清单、来源锁、能力与所有权、路径守卫、结构化执行、证据、Hook 安全迁移、内置合同探针和维护命令。
- 未接入 Forge、Grill、UmaDev 知识、Superpowers 方法、图形界面或九阶段真实控制。

最终候选身份由内置合同探针在全部受版本控制文件写定后生成。证据位于 `.super-dev/extensions/runs/<run-id>/result.json`，运行目录不进入 Git；候选任一代码、配置、测试或正式文档变化后，旧证据自动失效。

## 验收证据

- 扩展和 Hook：46 项通过，Windows 下 1 项 POSIX 专用测试跳过。
- 配置回归：79 项通过。
- Windows 安全完整单元基线：2414 项通过、19 项历史失败、4 项跳过、0 个错误；失败清单与阶段 0 登记一致，没有新增失败。
- 真实用户目录：前后变化 0；安全证据完整。
- Linux Python 3.13 只读容器：执行器 7 项全部通过，包括忽略普通结束信号的孙进程清理。
- 静态检查：本次改动范围 Ruff、类型检查、字节码编译和差异格式检查通过。
- 规格检查：质量 100 分，格式验证通过。
- 安装包：wheel 构建通过，并包含内置清单、来源锁和合同探针。
- 独立复审：Grok Build 首轮提出 3 个强制问题，修复后复审通过；Linux 实测又发现僵尸进程误判，修复并实测后再次窄复核，结论为 `VERDICT=ACCEPT`，无强制问题。

完整单元基线证据：`.super-dev/runs/minimal-extension-platform-unit/result.json`。19 项历史失败仍属于阶段 0 已分类问题，不能混入本阶段顺手修复。

## 回滚

1. 运行时立即回滚：保持 `extensions.enabled: false`，平台不会加载扩展；这是当前默认值。
2. 代码回滚：阶段 1 形成独立提交后，使用 Git 对该提交执行反向提交；不要改写主分支历史。
3. Hook 迁移回滚：随同阶段 1 提交整体反向提交，不单独恢复 `shell=True` 的旧命令字符串执行。
4. 证据清理：`.super-dev/extensions/` 只含被 Git 忽略的运行证据，可以在确认不再审计后单独清理，不影响主工作流状态。

## 交付边界

- 未修改用户级 Skill、宿主全局配置或凭据。
- 未提交、合并、推送、创建 PR 或部署。
- 提交和推送仍需用户单独授权。
