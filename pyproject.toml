[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "super-dev"
version = "2.5.0"
description = "Super Dev - Pipeline AI Coding Assistant"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    {name = "Excellent", email = "11964948@qq.com"}
]
keywords = [
    "ai",
    "development",
    "agent",
    "workflow",
    "claude",
    "cli"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development",
    "Topic :: Software Development :: Build Tools",
]

dependencies = [
    "packaging>=23.0",
    "rich>=13.0.0",
    "pyyaml>=6.0.0",
    "ddgs>=9.0.0,<10.0.0",
    "requests>=2.31.0",
    "defusedxml>=0.7.1",
    "openai>=1.0.0,<2.0.0",
    "fastapi>=0.110.0,<1.0.0",
    "uvicorn>=0.27.0",
    "tomli>=2.0.0; python_version<'3.11'",
    # Security floors for existing transitive runtime dependencies (2026-09-06 audit).
    "click>=8.3.3",
    "idna>=3.15",
    "starlette>=1.3.1",
    "urllib3>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "setuptools>=68.0",
    "pillow>=12.3.0",
    # Security floors for pip-audit's existing transitive dependencies.
    "pip>=26.2",
    "msgpack>=1.2.1",
    "pip-audit>=2.7.0",
    "types-requests>=2.31.0",
    "bandit>=1.7.9",
    "build>=1.2.0",
    "twine>=5.0.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-timeout>=2.3.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "types-PyYAML>=6.0.12",
]

[dependency-groups]
dev = [
    "setuptools>=68.0",
    "pillow>=12.3.0",
    # Keep the editable-install extra and uv development group aligned.
    "pip>=26.2",
    "msgpack>=1.2.1",
    "pip-audit>=2.7.0",
    "types-requests>=2.31.0",
    "bandit>=1.7.9",
    "build>=1.2.0",
    "twine>=5.0.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-timeout>=2.3.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "types-PyYAML>=6.0.12",
]

[project.urls]
Homepage = "https://github.com/mixyoung/super-dev"
Documentation = "https://github.com/mixyoung/super-dev#readme"
Repository = "https://github.com/mixyoung/super-dev"
Issues = "https://github.com/mixyoung/super-dev/issues"

[project.scripts]
super-dev = "super_dev.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["super_dev*"]

[tool.setuptools.package-data]
"super_dev" = ["data/design/*.csv", "templates/*.md", "templates/*.yaml", "web/frontend/dist/*"]
"super_dev.experts" = ["builtin/*.md", "playbooks/*.md"]
"super_dev.reviewers" = ["rules/*.yaml"]
"super_dev.extensions" = ["*.yaml", "builtins/*.yaml"]

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.uv]
default-groups = ["dev"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "super_dev.cli_analysis_mixin",
    "super_dev.cli_design_mixin",
    "super_dev.cli_governance_mixin",
    "super_dev.cli_host_ops_mixin",
    "super_dev.cli_release_quality_mixin",
    "super_dev.cli_spec_mixin",
    "super_dev.creators.document_generator_content_mixin",
    "super_dev.integrations.manager_content_mixin",
]
disable_error_code = ["attr-defined"]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore:The 'app' shortcut is now deprecated\\.:DeprecationWarning:httpx\\._client",
]
