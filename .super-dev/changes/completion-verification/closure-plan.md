# 四文档与 fork 更新体验收尾

用户于 2026-09-07 回复“按照建议执行”。基线为 main `59298d3`。
本轮继续 completion-verification，不覆盖原 tasks、确认记录或真实候选的用户裁决。

## 批次 A：兑现已确认文档

- [x] 运行前说明原因、计划、范围与权限；默认简洁首屏，详细信息按需展开，JSON 无提示混入。
- [x] 清单、来源、阶段、权限及路径前置失败形成同一 run_id 的 Core 阻断回执与指标；不可信清单不获得写入权限，证据目录不安全时明确未落盘。
- [ ] 核心扩展验证覆盖 Windows Python 3.10–3.13、Linux 3.13；继续用户目录隔离及前后快照。

## 批次 B：统一更新链（实现前冻结的补充设计）

- [x] 固定 mixyoung/super-dev GitHub 稳定 Release 为启动提示、检查、更新的唯一来源；缓存按来源隔离，不回退原版 PyPI。
- [x] 校验标签、版本、制品哈希和 Python 兼容性；识别 pip/uv 工具安装，源码或 editable 安装停留在明确的手工更新说明，不操作 Git。
- [x] 安装后用新解释器验证版本及资源；迁移/Skill 刷新不得复用旧进程模板。项目优先，用户级变更必须显式选择。
- [x] 失败如实区分未安装、安装成功但后处理受阻；锁文件不强杀宿主，不掩盖错误。
- [x] 隔离测试覆盖成功、失败、来源错配、损坏制品、旧进程、安装形态及用户目录边界。

## 最终验证

- [ ] 定向回归、类型、格式与贡献范围检查；远程平台结果以真实 CI 为准。
- [ ] 全部代码冻结后运行既有 10 场景回放和专项验收；不更改计时口径、正确答案、质量阈值及用户确认。
- [ ] 按原指标判断阶段 2B；未达标继续阶段 2，不新增系统化调试运行扩展。

无新主入口、流程阶段、GUI、模型程序或新发布。逐批可回退代码差异；历史证据保留。

最终复跑在此记录冻结之后进行，结果写入 output/verification-closure/ 与扩展证据目录。
不在复跑后修改本记录，以免再次改变被测代码版本；未勾选的跨平台项必须有实际 CI 才能结项。
