# UmaDev 文档、代码与模板软内容吸纳

## 用户授权

2026-09-06 用户要求“现在开始吸纳 umadev 的文档、代码、模板等偏思想和理念等软内容”。沿用已确认的宿主内、择优吸纳、保留 Super Dev 好的流程与功能的边界。

## 本批范围

六类：可验收需求表达、需求到验证追溯、风险驱动测试、测试完整性、带证据的评审、未决事项与上下文交接。源码和模板按固定提交查证，只改写适用原则到已有知识、专家、PRD 模板与标准 Skill。

## 非目标

不移植 UmaDev 可执行程序、模型调用、角色/任务调度、独立状态、向量服务或报告放行逻辑；不改质量分数与门禁；不覆盖此前方法吸收。当前 `completion-verification` 与 `super-dev-method-absorption` 的验收记录分别保留，不能混用本批证据。

## 验收

1. 每类原则有固定来源、现有重合、具体改写落点与不采纳项。
2. 知识可经现有检索获得，模板与标准 Skill 可经正常生成/安装链得到。
3. 需求写明行为与适用范围，不虚构指标、测试结果或已完成状态。
4. 风格建议不冒充阻断；测试/快照的合法变更不被一概禁止，但弱化验证不能支持通过。
5. 原九阶段、确认、SEEAI 与运行时保持原合同，通过定向回归和隔离行为演练。

## 回退

本批知识、专家与模板可独立回退，既有方法和旧证据保持可读。提交、推送、全局安装不在本次操作中。
