# 六项规则最小修正

用户于 2026-09-07 明确要求“不额外发散，仅使用最少变更修复以上问题”。基线 main 740f276，继续当前 completion-verification，不新建运行平台或流程。

1. 发布变更检查使用明确绑定的当前 change；缺少绑定时说明原因，不按目录时间猜，也不回退旧固定目录。
2. 治理文件目录发现改为不计分的资料提示，不把文件存在当通过。当前需求、合规、安全、测试等真实门禁仍保留；阈值与权重公式不改。
3. 同一 Hook/事件/阶段/来源的最近结果用于当前阻断判断；历史记录与历史计数保留，不让其他 Hook 的成功掩盖失败。发布报告和已有 Hook 汇总共用此判断。
4. 新流程与恢复会话分开说明：恢复读取并报告既有阶段，不重置为 research；新流程仍按原工作模式与确认门开始。
5. UI 工具链、设计 token 与页面骨架只约束涉及 UI 的工作；后端/CLI 仍需适用的契约、构建与验证。
6. release.sh 默认面向本 fork 的 GitHub 制品流程，不默认调用 PyPI 上传；显式发布绑定 mixyoung/super-dev。只隔离测试脚本，不实际发布。

只改对应实现/模板及必要重复合同，Skill 从唯一模板同步仓库副本。不调整 900 秒计划、不重跑全套旧交付、不改人工确认/历史账本、不顺带修其他旧债。本轮未授权提交、推送、合并或发布。

## 完成记录

六项均已实现。治理目录发现已移出计分，不改数值门槛和真实必需检查；Hook 发布判断与证据包共用最近同类结果，保留原始历史。Skill 从唯一模板同步，并定向对齐同一问题的宿主规则文案。发布脚本默认仅准备 GitHub 制品，显式发布绑定本仓库且附带既有更新器要求的校验和。

已完成针对性、关联回归与构建检查；详细通过数、旧夹具修订和未验证范围见同目录 adoption.md。本轮没有实际发布、提交或推送。
