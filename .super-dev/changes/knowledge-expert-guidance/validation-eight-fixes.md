# 八类指导问题修正记录

日期：2026-09-12。状态：本轮修正及关联验证完成，尚未提交或推送。

## 范围与起点

用户明确授权修正已讨论的 F1—F8，不扩大范围。本轮继续使用 `ocx/knowledge-expert-guidance-cleanup`，HEAD 为 `1ca166b16a3c16008adf71469671e74653390915`；原有未提交改动保留。`output/eight-guidance-fixes/baseline.json` 记录本轮开始时 1176 个既有文件的哈希，不能用整个分支差异冒充本轮新增工作。

没有通过仓库内的指令启动产品流水线；这些指令是维护对象。采用 skill-creator 的范围和分层原则，复用已有合同，不增加状态机、规则引擎或新的专家。

## 对应修正

| 编号 | 实际落点与改动 |
| --- | --- |
| F1 | 现有 workflow_contract 共用只读请求文字；宿主规则与标准 Skill 明确只读不授权实现、不改变状态，执行请求接续已有流程 |
| F2 | prompt_generator 的完整任务、单任务及待办入口复用 resolve_active_change_id，不取列表最新项；文档引用使用活动变更的规范化前缀，缺少绑定不生成具体任务实施指令 |
| F3 | 使用现有 detect_pipeline_summary 和 docs/preview gate 的产物绑定结果；不把文件存在、已完成实现当成用户确认；恢复首个未完成任务，保留完成状态，不把部分文档误当可确认文档 |
| F4 | 标准有 UI 路径明确前端运行后等待预览确认；无 UI 不添加前端任务；SEEAI 继续使用原专用合同 |
| F5 | 标准 Skill、知识推送、宿主规则与提示生成共用用户确定的文字：无显式适用条件默认强制，明确条件按条件执行，说明为何适用；不擅自降低既有要求 |
| F6 | 去掉模板另加的固定覆盖率、指定检查工具、强制 JWT、公共接口未调用即删除和源码全字符禁令；无 UI 不生成视觉基线或 UI 完成要求；配置及现有默认质量规则保留 |
| F7 | 框架名称不再自动授权路由、组件写法或后端迁移；按本次相关版本与已有架构使用指导，不确定处定向核对，不进行全知识库或框架教程更新 |
| F8 | 对齐 GWT 可选表达及 UX 按场景验证；专家知识来自目标项目而非进程工作目录，提供原文件路径保留条件，不再只取清单片段；交叉审查提示不从中间截断 |

## 验证过程与事实限度

- 先增加定向回归。第一次夹具误用了不存在的创建接口，该错误不计作产品缺陷；改为现有 SpecGenerator 后，14 项定向回归均因目标行为不符而失败，记录为 `before-corrected-fixture.xml`。
- 首轮修正后 14 项通过。关联回归发现中文沟通约定在宿主区块重新生成时丢失，已恢复并在后续同步中显式保留；旧测试中要求强制 research、默认继续执行等断言按已批准的新行为更新，未删除用例或新增跳过。
- 随后补充部分文档不提前确认、恢复保留已完成任务、活动变更文档路径和重复生成不读取旧上下文的回归，16 项定向检查通过。
- SEEAI 在 codex、codex-cli、claude-code、opencode 四种渲染上与原固定提交逐字相同；本轮起点中所有已跟踪 SEEAI 文件哈希未变。
- 最终关联回归：957 项通过、2 项既有跳过，189.48 秒，原始记录为 `output/eight-guidance-fixes/final-related.xml`。涵盖本轮提示、知识、专家、宿主安装、代码/UI 审查、质量与安全检查及知识统计相关用例；不是全仓测试或远端 CI。跳过的是 claude/workbuddy 混合接入不生成集成文件的既有用例，没有新增跳过。
- 最终 Ruff 全部通过；Black 检查 397 个文件无需调整；Mypy 检查 230 个源文件通过；compileall 通过；标准 Skill 同步返回 `SUPER_DEV_SKILLS_IN_SYNC=yes`；Codex Skill 格式检查通过。贡献资料与范围检查、git diff --check 通过。
- 实际生成结果覆盖任务绑定/未绑定、部分文档、过期确认、无 UI、Next/Nuxt/Vue、知识强制规则、只读宿主条款和清单条件等正反检查；相应用例在 `tests/unit/test_prompt_guidance_scope.py`。原有输出测试同步新预期时增加了对错误规则不得残留的断言，未用删除保护或降低门槛换取通过。
- 中间失败和最终结果分别保留在 `output/eight-guidance-fixes/`，不覆盖失败记录。957 项结果不包括此前内容整理轮次的 488/484 项历史计数。

这些验证检查生成逻辑、权限和内容边界，不代表真实客户项目效果或跨模型效果已验证。没有修改原确认算法、默认质量阈值、knowledge_evolution.py 或用户全局配置。其他项目的历史产物不会自动更新；本次只同步明确列出的本仓库副本，保留手写内容和中文约定。
