# 最小扩展平台任务表

## A. 合同与配置

- [x] A1. 增加扩展状态、能力、来源、所有权、命令、候选、事件和结果数据模型。
- [x] A2. 实现严格清单解析，拒绝未知字段、未知枚举和缺失必填字段。
- [x] A3. 实现内置来源和外部改编来源的条件字段校验。
- [x] A4. 增加 `extensions.enabled` 和 `allowed_builtin_methods` 配置校验，默认关闭。
- [x] A5. 增加合法与非法清单测试。

## B. 来源锁

- [x] B1. 实现文件稳定 SHA-256 摘要。
- [x] B2. 实现目录按规范化相对路径排序的稳定摘要。
- [x] B3. 实现清单与来源锁一致性检查。
- [x] B4. 摘要漂移时返回阻断且不自动刷新来源锁。
- [x] B5. 创建内置合同探针清单和来源锁。

## C. 所有权与能力

- [x] C1. 冻结 `read_files`、`run_commands`、`write_evidence` 最小能力词表。
- [x] C2. 实现可信能力与扩展申请求交集。
- [x] C3. 缺少能力时返回不适用。
- [x] C4. 拒绝生命周期、编排、生产写入和安排权。
- [x] C5. 拒绝 Git、PR、部署、外部写入和用户全局安装权限。
- [x] C6. 验证扩展自报风险或能力不能提升权限。

## D. 路径守卫

- [x] D1. 复用并扩展 Windows 真实路径、大小写和盘符身份判断。
- [x] D2. 拒绝 `..`、符号链接和目录连接逃逸。
- [x] D3. 拒绝 UNC、不同盘符和仓库外写入。
- [x] D4. 永久禁止 `.git`、主工作流状态和真实用户目录。
- [x] D5. 覆盖不存在目标、中文路径和空格路径。

## E. 候选与证据

- [x] E1. 生成仓库、基础提交、当前提交和工作区身份。
- [x] E2. 生成未提交差异、暂存内容和未跟踪文件清单摘要。
- [x] E3. 组合稳定候选摘要。
- [x] E4. 候选变化时判定旧证据失效。
- [x] E5. 实现原子 `result.json` 和追加 `history.jsonl`。
- [x] E6. 实现五类最小扩展事件。
- [x] E7. 损坏历史行跳过并返回警告。
- [x] E8. 验证扩展不能直接写历史或主工作流状态。

## F. 结构化执行器

- [x] F1. 实现 `CommandSpec` 程序和参数列表合同。
- [x] F2. 拒绝整段命令、Shell、`.cmd`、`.bat`、`.ps1` 和 Shell 脚本。
- [x] F3. 固定工作目录并构造最小环境白名单。
- [x] F4. 实现输出限长、摘要和敏感信息脱敏。
- [x] F5. 实现退出码、启动失败、超时和取消结果映射。
- [x] F6. Windows 使用 Job Object 管理完整进程树。
- [x] F7. POSIX 使用独立会话或进程组管理完整进程树。
- [x] F8. 超时、取消和异常后验证无存活子进程。

## G. Hook 安全迁移

- [x] G1. Hook 模型支持结构化程序和参数列表。
- [x] G2. 新配置型命令 Hook 通过结构化执行器运行。
- [x] G3. 旧命令字符串不执行并返回阻断和迁移示例。
- [x] G4. 代码内可信 Python 回调保持兼容。
- [x] G5. 日志 Hook 保持兼容。
- [x] G6. YAML `type: python` 返回明确迁移说明。
- [x] G7. 更新 Hook 示例和相关测试。

## H. 内置探针与 CLI

- [x] H1. 实现只通过 Core 证据接口写入的合同探针。
- [x] H2. 实现 `extension validate`。
- [x] H3. 实现 `extension inspect`。
- [x] H4. 实现 `extension verify-source`。
- [x] H5. 实现 `extension probe-contract`。
- [x] H6. 实现 `extension history --limit`。
- [x] H7. 实现中文摘要和 0/2/3/4/5 稳定退出码。

## I. 兼容与安全验证

- [x] I1. 扩展配置缺失和显式关闭均不加载扩展。
- [x] I2. 除旧 Hook 安全迁移外，关闭扩展与当前 Core 行为等价。
- [x] I3. 测试统一使用临时用户目录，真实用户位置前后变化为 0。
- [x] I4. 运行扩展相关单元和集成测试。
- [x] I5. 运行 Hook、配置、Skill、CLI 和 Windows 相关回归。
- [x] I6. 运行完整单元检查并分类既有 Windows 失败。
- [x] I7. 运行 Ruff、类型检查、字节码编译和差异检查。
- [x] I8. 验证没有真实外部方法、知识导入、图形界面或九阶段控制接线。
- [x] I9. 进行独立只读审查并修复强制问题。

## J. 交付边界

- [x] J1. 未修改用户级 Skill、宿主全局配置或凭据。
- [x] J2. 未自动提交、合并、推送、创建 PR 或部署。
- [x] J3. 验收证据绑定最终候选并记录回滚方式。
