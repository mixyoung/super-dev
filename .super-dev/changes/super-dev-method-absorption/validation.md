# 首批方法吸收验证记录

日期：2026-09-06。代码基线 `26bb222` 加本次未提交修改。范围只覆盖价值判断、需求缺口、业务概念的知识/专家/模板/Skill 接线；不代表整个项目发布就绪。

## 自动验证

- 环境：仓库 `.venv`，Python 3.13.12；未安装或变更全局依赖。
- pytest：**95 passed**，0 failures / 0 errors，10.95 秒。原始 JUnit：`output/super-dev-method-absorption-pytest.xml`。
- 范围：`test_method_absorption.py`、`test_document_generator_enhanced.py`、`test_super_dev_skill_contracts.py`、`test_expert_service.py`、`test_engine_knowledge.py`。
- 测试进程使用临时用户目录；安装链测试只写临时 home。确认实际安装内容等于当前唯一模板，不创建主流程状态。
- 通过真实 PRD 生成接口检查不再填充无依据的认证术语，缺陷修复的价值章节保持修复范围；真实知识推送在 research/docs 查询中命中更新条目。
- Ruff：`super_dev` 与新增测试通过；Python 编译通过；标准 Skill 两份 Codex 入口校验通过，四份仓库内副本同步通过。
- 标准 Skill 源码和新增测试的 Black 检查通过。大生成文件的整文件 Black 仍失败；用 HEAD 原文确认修改前也失败，本次改动的三个函数均与格式化结果一致。
- Mypy：Skill 模板独立检查通过；联合检查在大生成文件报告 16 项错误。使用 HEAD 原文及 `--shadow-file` 对照，基线也有相同 16 项错误，本次没有新增类型错误；整体类型检查仍不通过。
- SEEAI 的 Codex/Claude 渲染结果与 HEAD 逐字一致，本次不改其专属流程。

首次测试有两项失败：新增安装测试误用不存在的 `SkillInstallResult.success` 字段。已改为检查实际路径、隔离范围及安装内容，再得到上述 95 项通过结果；未放宽生产行为。

## 隔离上下文行为演练

由只读子代理 `/root/method_forward_test` 单独读取当前标准 Codex Skill、产品方法知识以及 PM/ARCHITECT 定义；未提供预期答案、测试代码或父会话推理。要求它针对六个独立场景给出首条业务回复，禁止实际修改案例项目。

### A：价值尚未验证

输入：想花两周为团队开发全天候 AI 经营报告，但只有提出者表达需求，已有报表导出，无明确读者，未试人工摘要。

实际回复：建议先用两三天做人工摘要试验；指出可导出数据不能证明需要全天候报告，提出观察读者行动、节省时间和摘要成本；询问最可能的读者与决策，最终由用户决定继续、缩小或暂缓。

观察：符合价值判断目标；未假称市场验证完成，未替用户否决项目。

### B：明确的学习目标

输入：两天完成本机图片批量校验工具，学习 Python 异步，无账户和服务器；成功标准与范围已确认，继续技术方案。

实际回复：无需补充商业价值证明；直接建议目录扫描、受限并发校验、汇总结果的模块方案，说明阻塞 I/O 与解码的处理以及验证重点。

观察：正确接受学习目标，没有重复立项论证。

### C：关键需求缺口

输入：已确认订单 CSV 的列、1000 条上限、UTF-8 和空列表行为；已有操作员/管理员角色，但未定义各自导出范围。

实际回复：指出导出权限缺口，建议操作员仅本人、管理员全部，越权明确报错；还指出超过 1000 条是拒绝还是截取需明确，其余已知格式复用。

观察：找到影响实现的权限和边界问题，没有重问已回答的列或编码。

### D：已充分定义的需求

输入：上述 CSV 需求补齐操作员本人、管理员全部、越权 403，并明确交付正常、空数据、越权和边界测试；请求进入原流程 Spec 拆分。

实际回复：无需再补充需求；按权限、查询范围与限制、CSV 输出、回归测试拆分四项任务，引用原验收条件。

观察：复用已确认材料，没有启动新的价值审查。

### E：概念与实现冲突

输入：PRD 允许部分取消；代码将全部订单商品取消，付款模块把 cancelled 解释为全额退款，词表没有区分二者。

实际回复：用已付款两件商品只取消一件的场景指出当前全额取消/退款违背需求，追问退款是否自动发生，建议先区分订单取消、商品取消与退款，再对齐需求和架构。

观察：识别业务语义冲突，没有把改名字当作修复。

### F：有明确映射的不同概念

输入：Customer 是合同客户，User 是员工，Payer 可由第三方代付，历史 account_id 已注明对应 Customer；询问是否应全改为 user。

实际回复：不应合并这三种身份；保留已注明的 account_id 映射，仅在实际调用违背词表时提出具体修改。

观察：接受合理别名与不同业务身份，没有机械统一名称。

## 证据局限

以上是一个隔离上下文、六个场景的首答演练，由本任务对照既定目标复核。不是跨模型 A/B 试验，不证明真实用户全流程、所有宿主或所有平台均已通过；实际文档落盘、长期恢复和真实项目效果仍需后续试用。演练为初步行为证据，和 pytest 的结构/接线验证分别报告。

完整六例原始回复保留在本任务的子代理回执；本文件记录输入和语义摘要，不将摘要伪装成逐字转录。`output/` 的 JUnit 是本地原始证据，本文随当前范围保存可读结果。
