# Forge、需求审查与 Grill：多轮闭环验证

日期：2026-09-06。范围为用户已授权的择优方法吸纳；继续同一变更，未移植第三方命令、角色程序或状态机。

## 实际落实

- Forge 补齐澄清/检验/改进目的、中心主张、关键追问与反证、新信息修正、备选方案、结论回写与恢复。
- 需求审查补齐原编号逐项对照、问题来源与影响、关键决定、定向修订、验收与已有设计/任务复核。
- Grill 补齐按本轮设计依赖讨论词义、规则、接口和重要取舍，决定写回原文档，再用场景复核；不再限于术语表。
- 上述步骤已进入现有产品知识、PM/ARCHITECT 指导、PRD 模板和标准 Super Dev Skill；无知识文件的安装场景也有自包含步骤。

## 自动检查

- 仓库 `.venv` Python 3.13.12；**100 passed**，0 failures/errors/skipped，终端总耗时 9.20 秒；JUnit 位于 `output/super-dev-method-closure-pytest.xml`。
- 覆盖原 95 项生成器、知识、专家、Skill 同步/隔离安装检查，以及原九阶段合同和活动变更确认绑定测试。
- Ruff 通过；Skill 模板和测试格式检查通过；生成文件中改动的三个函数与 Black 结果一致。大文件原有整文件格式问题不由本轮扩散修复。
- Skill 模板的 Mypy 通过，Codex Skill 校验与四份受控副本同步通过。大生成文件原有 16 项类型问题仍单独记录，不声称全仓类型门禁通过。
- Codex/Claude 的 SEEAI 渲染结果与 HEAD 原文逐字一致；工作流合同、门禁实现、影子策略和扩展运行代码没有修改。

## 三个隔离的多轮文档实操

使用 skill-creator 的隔离前向验证方式，三名执行者分别读取同一份标准 Skill 与方法知识。每轮只给当轮案例信息，执行者先提问，再收到新增事实或文档授权；未提供预期输出或让其自行编造用户回答。

完整初始文件、修改后文件、输入摘要、逐文件 SHA-256 与保留项检查已存于 `output/super-dev-method-closure-evidence.json`。输入摘要明确标为摘要；完整提示和原始回复仍可从当前任务的原生子代理记录复查。

| 方法 | 轮次 | 实际过程 | 结果 |
| --- | --- | --- | --- |
| Forge | 4 | 首轮识别日报频率与全天候说法的疑问；新信息说明实际是随时查看后修正判断；再根据人工阈值与经理决策确认范围，获授权更新原调研；最后恢复时直接承接已有决定 | 完成调研回写及回读；保留 F-KEEP，效率收益仍是待验证；正确回到三文档及原确认流程 |
| 需求审查 | 3 | 发现操作员权限与静默截断冲突；收到授权后更新原 FR/AC 与架构；再次只读复核每项发现和原确认状态 | 保留 FR-1 至 FR-4、原 AC 编号、UTF-8/空表头及归档非目标；1000允许、1001返回422，越权403；文档仍待确认 |
| Grill with Docs | 4 | 先列取消粒度→发货→退款→接口依赖；收到粒度和金额决定后继续讨论失败退款；再获授权写回词表、PRD、架构，以两件取消一件及退款失败复核；最后区分文档已完成、确认未完成、代码待实现 | 保留 Customer/account_id 映射和无关条款，无新 ADR；代码与状态原样，未将文档一致性误报为功能已修复 |

Forge 的新信息由本测试分轮提供，三天试点反馈属于案例输入，不是产品用户实证。Grill 最后一轮未找到 Payer 定义时明确说明未核验，没有为了迎合输入而捏造已有文档。

## 可机器复查的边界

- 三个案例目录共十个初始文件，结束时未新增或丢失文件。
- 只修改获授权的六份原文档：1 份调研、2 份导出文档、3 份取消设计文档。
- 三份案例流程状态保持原样，分别仍为 research、docs_confirm、architecture_revision，确认均为 false。
- 案例 `src/cancel.py` 内容不变；各案例的非目标和保留条款均存在。
- 真实仓库 `completion-verification` 的工作流、阶段记录、任务及三份核心文档共六份文件哈希保持不变。

## 结论与局限

在本轮限定范围内，三个方法的核心多轮过程已补齐并通过文档实操与原流程兼容检查；不是只增加方法名称或泛化原则。既有 6 个首答案例记录保留在 `validation.md`，不替换为本轮结果。

这仍是隔离案例验证，没有部署业务代码，不代表真实客户项目、所有宿主、所有操作系统或全项目发布门禁均通过。真实项目试用继续按原计划进行；未安装第三方框架、未改用户级配置、未提交或推送。
