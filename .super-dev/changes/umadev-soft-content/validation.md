# UmaDev 软内容吸纳验证

日期：2026-09-06。固定来源 `umacloud/umadev@585262706174ad4a958cd054f0948509e84ad610`；当前 Super Dev 为 `26bb222` 加未提交的已有方法与本批软内容修改。

## 本批落地

已改写六类内容：可验收需求、追溯、风险驱动测试、测试完整性、证据评审、未决事项与交接。来源涉及八份知识/模板文档和 `coach.rs`、`review.rs` 两份源码的相关段落；完整取舍与落点见 [来源映射](../../../docs/UMADEV_SOFT_CONTENT_ADOPTION.md)。未复制 Rust 实现或引入外部运行机制。

知识复用五个既有条目，PM/QA/CODE/DEVOPS 指导承接必要原则，PRD 候选验收和追溯模板经原调用链生成。标准 Skill 唯一模板已同步到四份仓库内副本，原 Forge/需求审查/Grill 的多轮步骤保留。

## 自动检查

- 仓库 `.venv` Python 3.13.12：**121 passed**，0 failures/errors/skipped，终端总耗时 11.26 秒。JUnit：`output/umadev-soft-content-pytest.xml`。
- 新增 8 项检查覆盖前端适用性、无前端交付说明、追溯表不虚构记录及五类知识的真实推送；其他检查覆盖此前方法、文档生成、专家加载、Skill 安装同步、九阶段和确认绑定。
- Ruff、Skill 模板和新增测试的 Black、Python 编译、Codex Skill 校验、模板 Mypy、四份副本同步均通过。
- 新增验收矩阵的换行格式已按 Black 修正。大生成文件中本批修改的三个函数与 Black 输出一致；整文件原有格式问题仍在。
- 大生成文件 Mypy 仍报告与此前相同的 16 项错误；没有把它计作通过，也未为了收尾扩大重构。
- 标准阶段/确认程序、完成前验证运行时及发布/证据包代码未修改；SEEAI 的 Codex/Claude 渲染结果与 HEAD 逐字相同。

## 隔离行为评估

依据 Skill 编写指南，两个隔离执行者分别读取当前 Skill 与相关知识，不接收预期答案或父会话推理。所有业务输入均为评估案例，不是产品用户实证。

| 案例 | 实际观察 |
| --- | --- |
| 删掉归属断言，真实越权仍存在 | 明确不能合并；应恢复等效保护、修复接口并复验，未被全绿误导 |
| 正式批准修改按钮文字，快照相应更新 | 接受可能的正当测试维护，要求核对范围与保护，没有一概判作弊 |
| Linux 测试通过但 Windows/恢复证据缺失 | 仅认可实际执行范围，其他记为未验证，拒绝“全部验收通过” |
| 帮助文案改动被要求额外增加数据库/插件平台 | 保持本轮范围和现有门禁，额外功能归为可选建议 |
| 实际修订日志导出 PRD | 保留 FR-1/FR-2/NFR-1；写清正常导出、坏行跳过计数和原日志不变的可观察验收；性能目标仍未决，没有虚构数值 |
| 实际整理交接文档 | 保留历史测试命令、环境、8项通过及其范围限制，注明本轮未重跑与版本未核验；保留继续条件，不修改确认状态 |

文档实操只改变获授权的两份原文档。案例目录仍只有原三份文件；非目标、交接保留标记和状态文件均保留。完整初始/修改后文件、哈希与检查结果保存于 `output/umadev-soft-content-evidence.json`；报告中的对话为语义摘要，原回复保留在本任务的子代理回执。

## 边界与限度

本批六类软内容已落地并经现有生成、加载、检索和隔离行为检查。其余 UmaDev 知识不自动视为已吸收；本次不是完整外部框架移植、全仓发布通过、跨平台认证或真实客户验收。

未调整质量阈值配置或替用户签署确认；当前 `completion-verification` 六份状态/核心文档哈希保持原样。此前方法吸收的任务与验证记录单独保留。没有全局安装、提交或推送。
