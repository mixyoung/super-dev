# 完成前验证试点任务表

## A. 已确认基础

- [x] A1. 完成基线、调研、需求、技术方案和使用体验文档。
- [x] A2. 用户确认最终四份文档。
- [x] A3. Grok Build 最终复审为 `VERDICT=ACCEPT`，可选建议已经关闭。
- [x] A4. 明确唯一执行入口、当前 Python＋pytest、每次重跑、唯一运行编号和用户独立验收。
- [x] A5. 完成来源锁 LF/CRLF 稳定摘要前置整改及定向验证。

## B. 配置与数据合同

- [x] B1. 扩展配置允许 `fresh-verification`，默认仍关闭。
- [x] B2. 严格校验 `profile`、`plan_id`、pytest 参数和超时，拒绝未知字段。
- [x] B3. 拒绝任意程序、Shell、绝对输出路径、父目录逃逸和覆盖 Core 固定参数。
- [x] B4. 增加测试摘要（`PytestSummary`）及只读派生实际执行数。
- [x] B5. 增加验证提醒（`VerificationAdvisory`）及结构化序列化。
- [x] B6. 验证首版只有运行编号（`run_id`），没有第二套同义编号。

## C. 内置来源与权限

- [x] C1. 创建 `fresh-verification` 内置清单和来源实现。
- [x] C2. 更新来源锁的来源摘要和清单摘要。
- [x] C3. 固定 `run_commands`、`write_evidence` 能力和 delivery 阶段。
- [x] C4. 拒绝生命周期、编排、生产写入、Git、部署、外部写入和全局安装权限。
- [x] C5. 验证来源或清单漂移时阻断且不自动刷新。

## D. JUnit 与提醒

- [x] D1. 解析单套件与多套件 JUnit，拒绝缺失、损坏、负数和矛盾统计。
- [x] D2. 从同一份 JUnit 得到测试总数、失败、错误、跳过和耗时。
- [x] D3. 只用“测试总数减跳过数”派生并序列化实际执行数。
- [x] D4. 固定注入 `-rX`，只解析简短测试摘要区的 `XPASS` 协议记录。
- [x] D5. 多项非严格意外通过聚合成一条 `NON_STRICT_XPASS` 提醒并记录次数。
- [x] D6. 验证原因文本、任意日志、缓存和历史输出不会生成提醒。

## E. 新鲜验证执行

- [x] E1. 每次调用生成新的运行编号并记录运行前候选。
- [x] E2. Core 固定当前 Python、JUnit、临时目录、缓存关闭和最小隔离环境。
- [x] E3. 通过结构化执行器运行 pytest，不使用 Shell。
- [x] E4. 映射 pytest 退出码 0-6、启动失败、超时、取消和进程清理状态。
- [x] E5. 运行后重新计算候选，变化时使本次证据阻断。
- [x] E6. 原子写入 `result.json`、`pytest.xml` 和 `pytest-summary.json`。
- [x] E7. 追加扩展事件和等待复核的试点指标，不自动填写最终验收标签。
- [x] E8. 固定 pytest 临时项目的 Git 上探边界，防止继承真实父仓库。

## F. 发布就绪与命令行体验

- [x] F1. 只有 CLI `super-dev release readiness` 显式请求新验证。
- [x] F2. proof-pack、quality、Web API 和其他评估器调用保持旧行为。
- [x] F3. 试点未启用时保留旧行为和 `--verify-tests` 兼容路径。
- [x] F4. 试点启用后不重复运行旧 `--verify-tests` 路径。
- [x] F5. 发布报告增加严重级别为关键的完成前验证检查和机器可读证据。
- [x] F6. 命令行第一屏显示中文结论、数量、影响、下一步和证据路径。
- [x] F7. 非严格意外通过时显示中文提醒，不把提醒写成失败或项目完成。

## G. 单元与集成测试

- [x] G1. 配置合法、缺失、未知字段、危险参数和关闭兼容测试。
- [x] G2. JUnit 单套件、多套件、损坏、矛盾、普通跳过、预期失败、严格/非严格意外通过和全部跳过测试。
- [x] G3. 固定摘要区解析、任意日志拒绝、提醒聚合和输出摘要绑定测试。
- [x] G4. 当前候选通过、失败、无测试、超时、取消和候选变化集成测试。
- [x] G5. 同一候选连续调用产生不同运行编号且不复用旧结果。
- [x] G6. 发布就绪唯一入口、证据包和 Web API 不触发测试的回归测试。
- [x] G7. 真实用户目录、Git、主工作流状态和外部系统零变化测试。
- [x] G8. 覆盖仓库内 `pytest-temp` 的临时项目不继承父仓库，同时真实项目根目录仍能识别当前 Git。

## H. 指标与试点验收

- [x] H1. 完成 10 个回放场景的定义、真实执行和旧/新流程对照。
- [x] H1.1. 已定义 10 条有明确预期状态的回放场景。
- [x] H1.2. 真实执行并记录 10 个旧/新流程对照与两种验收时间。
- [x] H2. 在运行前登记 3 个真实候选的名称、范围和独立验收人。
- [x] H2.1. 候选 1 已预登记并完成用户验收：首次误阻断，修复后正确通过。
- [x] H2.2. 候选 2 已预登记并完成用户验收：正确通过。
- [x] H2.3. 候选 3 已预登记并完成用户验收：正确通过。
- [x] H3. 真实候选默认标记等待复核（`unreviewed`），只允许用户写最终标签。
- [x] H4. 等待复核不进入比率、时间指标或真实候选完成数。
- [x] H5. 汇总虚假完成、误阻断、正确验收时间、修正轮次、验证耗时和候选变化。
- [x] H6. 未达到阶段 2B 阈值时不建议接入系统化调试。

## I. 质量与交付边界

- [x] I1. 运行 Ruff、类型检查、字节码编译和差异检查。
- [x] I2. 运行扩展、配置、发布就绪、CLI、Web API 和 Windows 安全回归。
- [x] I3. 运行完整单元检查并分类既有失败，不把失败伪装为通过。
- [x] I4. 进行 Grok Build 独立只读复审；没有必须问题，最终 `VERDICT=ACCEPT`。
- [x] I5. 未修改用户级 Skill、宿主全局配置或凭据。
- [x] I6. 未自动提交、合并、推送、创建 PR、部署或外部写入。
- [x] I7. 未接入系统化调试、TDD、Forge、Grill、知识导入或九阶段真实控制。

## J. 用户后续授权的伴随治理修复
- [x] J1. 完成活动变更解析、路径安全与当前产物前缀隔离。
- [x] J2. 文档和预览确认只绑定当前变更，变化后旧确认失效。
- [x] J3. 统一质量、发布和证据包前缀，失败或过期质量不计完成。
- [x] J4. 保持无活动变更的旧行为。
- [x] J5. 根项目固定为 `platform=cli`、`frontend=none`、`backend=python`。
- [x] J6. 跳过不适用的前端与预览门，根 Python 包满足后端，官网和示例独立。
- [x] J7. 九阶段名称不变且不写预览确认。
- [x] J8. 固化自然中文、术语双名、三状态、当前代码版本和代码格式合同。
- [x] J9. 同步根 CLAUDE、兼容镜像、唯一模板和受控生成副本。
- [x] J10. 修复 `compliance` 命令识别与审查导入，防止命令文本覆盖项目描述。
- [x] J11. 证据包复用已有发布就绪报告，不覆盖完成前验证证据。
